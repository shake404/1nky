import DefaultTheme from 'vitepress/theme'
import './style.css'

// Only the two branding overrides in style.css are custom here — everything
// else about the theme (layout, search, sidebar) stays the stock default.
export default DefaultTheme
